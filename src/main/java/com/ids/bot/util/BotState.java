package com.ids.bot.util;

/**
 * Logical page states the bot can occupy.
 * Five states mapping onto TechMarket's 14 pages.
 */
public enum BotState {
    HOME,
    PRODUCTS,
    PRODUCT_DETAIL,
    ABOUT,
    CONTACT
}
